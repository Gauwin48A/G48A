# Modules Layer

This folder is reserved for domain-oriented backend modules.

Suggested per-module structure:

- `routes/`
- `controller/`
- `service/`
- `schema/`
- `repository/`

The current codebase still has legacy folders (`controllers`, `routes`, `services`). New development should prefer module-first organization here.
