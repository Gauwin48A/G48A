# Shared Backend Utilities

Use this folder for backend cross-module concerns:

- shared validators
- shared mappers/serializers
- shared domain errors
- reusable helper abstractions

Keep module-specific logic inside `src/modules/<module-name>`.
