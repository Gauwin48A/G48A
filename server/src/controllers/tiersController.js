const pool=require("../config/db");const logger=require("../utils/logger");const cacheService=require("../services/cacheService");const DB_QUERY_TIMEOUT_MS=Number.parseInt(process.env.DB_QUERY_TIMEOUT_MS,10)||1e4;const TIER_CACHE_TTL_SECONDS=Number.parseInt(process.env.TIER_CACHE_TTL_SECONDS,10)||300;function runQuery(text,values=[]){return pool.query({text:text,values:values,query_timeout:DB_QUERY_TIMEOUT_MS})}const defaultTiers=[{tier_id:1,name:"Free",price:0,description:"Basic listing with limited features",max_images:1,color:"gray-400",icon:"FiDollarSign",tier_order:1},{tier_id:2,name:"Basic",price:1.99,description:"Standard listing with more visibility",max_images:3,color:"blue-400",icon:"FiShield",tier_order:2},{tier_id:3,name:"Featured",price:4.99,description:"Prominent listing with enhanced features",max_images:5,color:"yellow-400",icon:"FiStar",tier_order:3},{tier_id:4,name:"Premium",price:9.99,description:"Top-tier listing for maximum exposure",max_images:10,color:"purple-400",icon:"FiCrown",tier_order:4}];exports.getTiers=async(req,res)=>{try{const tiers=await cacheService.getOrSetWithStampedeProtection("tiers:all",async()=>{const result=await runQuery(`SELECT tier_id, name, price, description, max_images, color, icon, tier_order\n           FROM tiers\n           ORDER BY tier_order ASC`);return result.rows},TIER_CACHE_TTL_SECONDS);if(tiers&&tiers.length>0){res.json(tiers)}else{res.json(defaultTiers)}}catch(err){logger.error("[Tiers] Error:",err.message);res.json(defaultTiers)}};