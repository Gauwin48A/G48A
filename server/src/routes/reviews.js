const express=require("express");const router=express.Router();const{protect:protect}=require("../middleware/auth");const reviewsController=require("../controllers/reviewsController");router.get("/user/:userId",reviewsController.getReviewsForUser);router.post("/",protect,reviewsController.createReview);router.patch("/:reviewId/helpful",protect,reviewsController.markReviewHelpful);router.post("/:reviewId/respond",protect,reviewsController.respondToReview);router.post("/:reviewId/flag",protect,reviewsController.flagReview);router.patch("/:reviewId/moderate",protect,reviewsController.moderateReviewVisibility);router.delete("/:reviewId",protect,reviewsController.deleteReview);module.exports=router;