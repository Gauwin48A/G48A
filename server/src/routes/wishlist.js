const express=require("express");const router=express.Router();const{protect:protect}=require("../middleware/auth");const wishlistController=require("../controllers/wishlistController");router.use(protect);router.get("/",wishlistController.getWishlist);router.post("/",wishlistController.addToWishlist);router.get("/check/:postId",wishlistController.checkWishlist);router.delete("/:postId",wishlistController.removeFromWishlist);module.exports=router;