const express=require("express");const router=express.Router();const{protect:protect}=require("../middleware/auth");const{captchaMiddleware:captchaMiddleware}=require("../middleware/captcha");const referralController=require("../controllers/referralController");router.get("/",protect,referralController.getReferral);router.post("/create",protect,referralController.createReferral);router.post("/track",protect,captchaMiddleware("referral"),referralController.trackReferral);router.get("/leaderboard",referralController.getLeaderboard);module.exports=router;
