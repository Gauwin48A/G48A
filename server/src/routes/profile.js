const express=require("express");const router=express.Router();const multer=require("multer");const profileController=require("../controllers/profileController");const{protect:protect}=require("../middleware/auth");const upload=multer({storage:multer.memoryStorage(),limits:{fileSize:5*1024*1024},fileFilter:(req,file,cb)=>{if(file.mimetype.startsWith("image/")){cb(null,true)}else{cb(new Error("Only image files are allowed"))}}});router.get("/",protect,profileController.getProfile);const{body:body,validationResult:validationResult}=require("express-validator");const validateProfileUpdate=[body("email").optional().isEmail().withMessage("Invalid email format"),body("phone").optional().isMobilePhone().withMessage("Invalid phone number"),(req,res,next)=>{const errors=validationResult(req);if(!errors.isEmpty()){return res.status(400).json({errors:errors.array()})}next()}];router.post("/update",protect,validateProfileUpdate,profileController.updateProfile);router.post("/upload-avatar",protect,upload.single("avatar"),profileController.uploadAvatar);router.get("/preferences",protect,profileController.getPreferences);router.post("/preferences/update",protect,profileController.updatePreferences);module.exports=router;