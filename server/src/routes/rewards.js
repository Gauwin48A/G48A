const express=require("express");const router=express.Router();const{protect:protect}=require("../middleware/auth");const rewardsController=require("../controllers/rewardsController");const rewardController=require("../controllers/rewardController");router.use(protect);router.get("/",rewardsController.getRewards);router.get("/log",rewardsController.getRewardLog);router.get("/stream",rewardsController.streamRewardUpdates);router.get("/my",rewardController.getMyRewards);router.post("/redeem",rewardController.redeemRewards);router.get("/by-user",rewardsController.getRewardsByUser);router.get("/user/:userId",rewardsController.getRewardsByUser);module.exports=router;