const express=require("express");const router=express.Router();const{protect:protect}=require("../middleware/auth");const chatController=require("../controllers/chatController");router.use(protect);router.get("/conversations",chatController.getConversations);router.get("/conversations/:conversationId",chatController.getMessages);router.post("/send",chatController.sendMessage);router.get("/unread",chatController.getUnreadCount);module.exports=router;