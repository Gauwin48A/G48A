const express=require("express");const router=express.Router();const fcm=require("../services/fcm");const pool=require("../config/db");const logger=require("../utils/logger");const{protect:protect}=require("../middleware/auth");const DB_QUERY_TIMEOUT_MS=Number.parseInt(process.env.DB_QUERY_TIMEOUT_MS,10)||1e4;function runQuery(text,values=[]){return pool.query({text:text,values:values,query_timeout:DB_QUERY_TIMEOUT_MS})}function parseOptionalString(value){if(value===undefined||value===null)return null;const normalized=String(value).trim();return normalized||null}function getAuthenticatedUserId(req){return parseOptionalString(req.user?.userId||req.user?.id||req.user?.user_id)}function isAdminRole(role){const normalized=String(role||"").trim().toLowerCase();return normalized==="admin"||normalized==="superadmin"}async function hasAdminAccess(req){if(isAdminRole(req.user?.role)){return true}const userId=getAuthenticatedUserId(req);if(!userId)return false;try{const result=await runQuery(`\n                SELECT COALESCE(NULLIF(to_jsonb(u)->>'role', ''), 'user') AS role\n                FROM users u\n                WHERE u.user_id::text = $1\n                LIMIT 1\n            `,[userId]);return isAdminRole(result.rows[0]?.role)}catch(error){logger.warn("[Push] Admin access check failed",{message:error.message});return false}}router.post("/register",protect,async(req,res)=>{try{const userId=getAuthenticatedUserId(req);const{token:token,deviceType:deviceType="web",deviceName:deviceName}=req.body;if(!token){return res.status(400).json({error:"Token is required"})}if(!userId){return res.status(401).json({error:"User ID is required"})}await fcm.registerToken(userId,token,deviceType,deviceName);res.json({success:true,message:"Device registered for push notifications"})}catch(error){logger.error("Error registering device:",error);res.status(500).json({error:"Failed to register device"})}});router.delete("/unregister",protect,async(req,res)=>{try{const userId=getAuthenticatedUserId(req);const{token:token}=req.body;if(!token){return res.status(400).json({error:"Token is required"})}if(!userId){return res.status(401).json({error:"Authentication required"})}const result=await fcm.unregisterToken(token,userId);if((result?.deactivatedCount||0)===0){return res.status(404).json({error:"Token not found for user"})}res.json({success:true,message:"Device unregistered"})}catch(error){logger.error("Error unregistering device:",error);res.status(500).json({error:"Failed to unregister device"})}});router.post("/send",protect,async(req,res)=>{try{const canSend=await hasAdminAccess(req);if(!canSend){return res.status(403).json({error:"Admin access required"})}const{userId:userId,title:title,body:body,data:data}=req.body;if(!userId||!title||!body){return res.status(400).json({error:"userId, title, and body are required"})}const result=await fcm.sendToUser(userId,title,body,data||{});res.json(result)}catch(error){logger.error("Error sending notification:",error);res.status(500).json({error:"Failed to send notification"})}});router.post("/broadcast",protect,async(req,res)=>{try{const canBroadcast=await hasAdminAccess(req);if(!canBroadcast){return res.status(403).json({error:"Admin access required"})}const{title:title,body:body,data:data}=req.body;if(!title||!body){return res.status(400).json({error:"title and body are required"})}const result=await runQuery("SELECT token FROM device_tokens WHERE is_active = true");if(result.rows.length===0){return res.json({success:false,reason:"No registered devices"})}const tokens=result.rows.map(row=>row.token);const sendResult=await fcm.sendToMultiple(tokens,title,body,data||{});res.json(sendResult)}catch(error){logger.error("Error broadcasting notification:",error);res.status(500).json({error:"Failed to broadcast notification"})}});module.exports=router;