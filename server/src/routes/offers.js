const express=require("express");const router=express.Router();const{protect:protect}=require("../middleware/auth");const offersController=require("../controllers/offersController");router.use(protect);router.post("/",offersController.createOffer);router.get("/",offersController.getOffers);router.get("/history/:postId",offersController.getOfferHistory);router.put("/auto-accept",offersController.setAutoAcceptThreshold);router.patch("/:offerId",offersController.respondToOffer);module.exports=router;