const express=require("express");const router=express.Router();const{protect:protect}=require("../middleware/auth");const searchesController=require("../controllers/savedSearchesController");router.use(protect);router.post("/",searchesController.saveSearch);router.get("/",searchesController.getSavedSearches);router.get("/:searchId/matches",searchesController.getMatchingPosts);router.delete("/:searchId",searchesController.deleteSearch);router.patch("/:searchId/notifications",searchesController.toggleNotifications);module.exports=router;