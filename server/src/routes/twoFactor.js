const express=require("express");const router=express.Router();const twoFactorController=require("../controllers/twoFactorController");const{protect:protect}=require("../middleware/auth");router.use("/setup",protect);router.use("/verify",protect);router.use("/disable",protect);router.use("/status",protect);router.post("/setup",twoFactorController.setup2FA);router.post("/verify",twoFactorController.verify2FA);router.post("/disable",twoFactorController.disable2FA);router.get("/status",twoFactorController.get2FAStatus);router.post("/validate",twoFactorController.validate2FA);module.exports=router;