const express=require("express");const router=express.Router();const inquiryController=require("../controllers/inquiryController");const{protect:protect,optionalAuth:optionalAuth}=require("../middleware/auth");router.post("/",optionalAuth,inquiryController.createInquiry);router.get("/seller",protect,inquiryController.getInquiriesForSeller);router.get("/templates",protect,inquiryController.getQuickReplyTemplates);router.get("/analytics",protect,inquiryController.getInquiryAnalytics);router.get("/post/:postId",protect,inquiryController.getInquiriesForPost);router.patch("/:inquiryId/status",protect,inquiryController.updateInquiryStatus);router.post("/:inquiryId/reply",protect,inquiryController.replyToInquiry);router.patch("/:inquiryId/spam",protect,inquiryController.markAsSpam);module.exports=router;