const express=require("express");const router=express.Router();const{protect:protect}=require("../middleware/auth");const saleController=require("../controllers/saleController");router.use(protect);router.post("/initiate",saleController.initiateSale);router.post("/confirm",saleController.confirmSale);router.post("/cancel",saleController.cancelSale);router.get("/pending",saleController.getPendingSales);module.exports=router;