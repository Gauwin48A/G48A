const express=require("express");const router=express.Router();const{protect:protect}=require("../middleware/auth");const complaintsController=require("../controllers/complaintsController");router.get("/",protect,complaintsController.getComplaints);router.post("/",protect,complaintsController.createComplaint);router.get("/my",protect,complaintsController.getMyComplaints);router.patch("/:id/status",protect,complaintsController.updateComplaintStatus);router.patch("/:id/evidence",protect,complaintsController.addComplaintEvidence);module.exports=router;