const Pusher=require("pusher");require("dotenv").config();const isPusherConfigured=!!(process.env.PUSHER_APP_ID&&process.env.PUSHER_KEY&&process.env.PUSHER_SECRET&&process.env.PUSHER_CLUSTER);let pusher=null;if(isPusherConfigured){pusher=new Pusher({appId:process.env.PUSHER_APP_ID,key:process.env.PUSHER_KEY,secret:process.env.PUSHER_SECRET,cluster:process.env.PUSHER_CLUSTER,useTLS:true});console.log("📡 Pusher: Realtime enabled")}else{console.log("⚠️ Pusher: Not configured (will use Socket.io fallback)")}const triggerEvent=async(channel,event,data)=>{if(!pusher){console.log("[Pusher] Not configured, skipping event");return null}try{await pusher.trigger(channel,event,data);console.log(`[Pusher] Triggered ${event} on ${channel}`);return true}catch(error){console.error("[Pusher] Trigger failed:",error);return false}};const sendChatMessage=async(chatId,message)=>triggerEvent(`chat-room-${chatId}`,"new-message",{id:message.message_id,content:message.content,senderId:message.sender_id,createdAt:message.created_at});const sendNotification=async(userId,notification)=>triggerEvent(`user-${userId}`,"notification",notification);const getClientCredentials=()=>({key:process.env.PUSHER_KEY,cluster:process.env.PUSHER_CLUSTER});module.exports={pusher:pusher,isPusherConfigured:isPusherConfigured,triggerEvent:triggerEvent,sendChatMessage:sendChatMessage,sendNotification:sendNotification,getClientCredentials:getClientCredentials};