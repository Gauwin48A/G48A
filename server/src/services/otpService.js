const crypto=require("crypto");const pool=require("../config/db");const otpDeliveryService=require("./otpDeliveryService");const OTP_EXPIRY_MINUTES=10;const MAX_ATTEMPTS=3;const generateOTP=()=>crypto.randomInt(1e5,1e6).toString();const hashOTP=otp=>crypto.createHash("sha256").update(otp).digest("hex");const generateAndStoreOTP=async(userId,purpose="sale_confirm")=>{const otp=generateOTP();const hashedOTP=hashOTP(otp);const expiresAt=new Date(Date.now()+OTP_EXPIRY_MINUTES*60*1e3);try{await pool.query(`UPDATE otp_store SET is_used = true WHERE user_id = $1 AND purpose = $2 AND is_used = false`,[userId,purpose]);await pool.query(`\n            INSERT INTO otp_store (user_id, otp_hash, purpose, expires_at, attempts, is_used, created_at)\n            VALUES ($1, $2, $3, $4, 0, false, NOW())\n        `,[userId,hashedOTP,purpose,expiresAt])}catch(err){console.warn("[OTP] otp_store table not found, using in-memory fallback")}return otp};const verifyOTP=async(userId,code,purpose="sale_confirm")=>{const hashedCode=hashOTP(code);try{const result=await pool.query(`\n            SELECT\n                id,\n                user_id,\n                otp_hash,\n                purpose,\n                expires_at,\n                attempts,\n                is_used,\n                created_at\n            FROM otp_store\n            WHERE user_id = $1 AND purpose = $2 AND is_used = false\n            ORDER BY created_at DESC LIMIT 1\n        `,[userId,purpose]);if(result.rows.length===0)return{valid:false,error:"No OTP found. Please request a new one."};const rec=result.rows[0];if(new Date>new Date(rec.expires_at)){await pool.query("UPDATE otp_store SET is_used = true WHERE id = $1",[rec.id]);return{valid:false,error:"OTP has expired. Please request a new one."}}if(rec.attempts>=MAX_ATTEMPTS){await pool.query("UPDATE otp_store SET is_used = true WHERE id = $1",[rec.id]);return{valid:false,error:"Too many failed attempts. Please request a new OTP."}}await pool.query("UPDATE otp_store SET attempts = attempts + 1 WHERE id = $1",[rec.id]);if(rec.otp_hash!==hashedCode){const remaining=MAX_ATTEMPTS-rec.attempts-1;return{valid:false,error:`Invalid OTP. ${remaining} attempt(s) remaining.`}}await pool.query("UPDATE otp_store SET is_used = true WHERE id = $1",[rec.id]);return{valid:true}}catch(err){console.warn("[OTP] Verification fallback:",err.message);return{valid:false,error:"OTP verification service unavailable"}}};const sendOTP=async(channel,destination,otp,options={})=>{const message=`Your MHub verification code is: ${otp}. Valid for ${OTP_EXPIRY_MINUTES} minutes. Do not share this code.`;const flow=String(options.flow||"unknown");const purpose=String(options.purpose||"generic");const metadata=options.metadata&&typeof options.metadata==="object"?options.metadata:{};const{deliveryId:deliveryId}=await otpDeliveryService.startDeliveryRecord({flow:flow,purpose:purpose,channel:channel,destination:destination,metadata:metadata});const finalizeAndReturn=async(result,statusOverride=null)=>{const provider=result?.provider||null;const providerMessageId=result?.providerMessageId||result?.messageId||result?.sid||null;const sendStatus=statusOverride||(result?.mock?"mock":result?.success?"sent":"failed");await otpDeliveryService.finalizeDeliveryRecord({deliveryId:deliveryId,provider:provider,providerMessageId:providerMessageId,sendStatus:sendStatus,metadata:{...metadata,flow:flow,purpose:purpose}});return{...result,deliveryId:deliveryId,providerMessageId:providerMessageId}};if(channel==="sms"&&process.env.TWILIO_ACCOUNT_SID&&process.env.TWILIO_AUTH_TOKEN){try{const twilio=require("twilio")(process.env.TWILIO_ACCOUNT_SID,process.env.TWILIO_AUTH_TOKEN);const result=await twilio.messages.create({body:message,to:destination,from:process.env.TWILIO_FROM});console.log(`[OTP] Twilio SMS sent to ${destination}: ${result.sid}`);return finalizeAndReturn({success:true,provider:"twilio",sid:result.sid,providerMessageId:result.sid})}catch(err){console.error("[OTP] Twilio error:",err.message)}}if(channel==="sms"&&process.env.MSG91_AUTH_KEY){try{const https=require("https");const payload=JSON.stringify({flow_id:process.env.MSG91_FLOW_ID,sender:process.env.MSG91_SENDER_ID||"MHUBAP",mobiles:destination.replace(/\D/g,""),otp:otp});const msg91Response=await new Promise((resolve,reject)=>{const req=https.request({hostname:"api.msg91.com",path:"/api/v5/flow/",method:"POST",headers:{"Content-Type":"application/json",authkey:process.env.MSG91_AUTH_KEY}},res=>{let data="";res.on("data",chunk=>data+=chunk);res.on("end",()=>resolve(JSON.parse(data)))});req.on("error",reject);req.write(payload);req.end()});console.log(`[OTP] MSG91 SMS sent to ${destination}`);const msg91MessageId=msg91Response?.request_id||msg91Response?.requestId||msg91Response?.message_id||null;return finalizeAndReturn({success:true,provider:"msg91",providerMessageId:msg91MessageId})}catch(err){console.error("[OTP] MSG91 error:",err.message)}}if(channel==="email"&&process.env.SENDGRID_API_KEY){try{const sgMail=require("@sendgrid/mail");sgMail.setApiKey(process.env.SENDGRID_API_KEY);const sendGridResult=await sgMail.send({to:destination,from:process.env.SENDGRID_FROM||"noreply@mhub.app",subject:"Your MHub Verification Code",text:message,html:`<p style="font-family:sans-serif">Your MHub code is: <strong style="font-size:24px;letter-spacing:4px">${otp}</strong></p><p>Valid for ${OTP_EXPIRY_MINUTES} minutes.</p>`});console.log(`[OTP] SendGrid email sent to ${destination}`);const sendGridMessageId=sendGridResult?.[0]?.headers?.["x-message-id"]||null;return finalizeAndReturn({success:true,provider:"sendgrid",providerMessageId:sendGridMessageId})}catch(err){console.error("[OTP] SendGrid error:",err.message)}}if(channel==="email"&&process.env.SMTP_HOST){try{const nodemailer=require("nodemailer");const transporter=nodemailer.createTransport({host:process.env.SMTP_HOST,port:Number.parseInt(process.env.SMTP_PORT||"587",10),secure:process.env.SMTP_SECURE==="true",auth:{user:process.env.SMTP_USER,pass:process.env.SMTP_PASS}});const smtpResult=await transporter.sendMail({from:process.env.SMTP_FROM||process.env.SMTP_USER,to:destination,subject:"Your MHub Verification Code",text:message});console.log(`[OTP] SMTP email sent to ${destination}`);return finalizeAndReturn({success:true,provider:"smtp",providerMessageId:smtpResult?.messageId||null})}catch(err){console.error("[OTP] SMTP error:",err.message)}}console.log(`\n[MOCK OTP] ========================================`);console.log(`[MOCK OTP] Channel : ${channel.toUpperCase()}`);console.log(`[MOCK OTP] To      : ${destination}`);console.log(`[MOCK OTP] Code    : ${otp}`);console.log(`[MOCK OTP] Expires : ${OTP_EXPIRY_MINUTES} minutes`);console.log(`[MOCK OTP] ========================================\n`);await new Promise(resolve=>setTimeout(resolve,200));return finalizeAndReturn({success:true,mock:true,provider:"mock"},"mock")};module.exports={generateOTP:generateOTP,generateAndStoreOTP:generateAndStoreOTP,verifyOTP:verifyOTP,sendOTP:sendOTP,OTP_EXPIRY_MINUTES:OTP_EXPIRY_MINUTES,MAX_ATTEMPTS:MAX_ATTEMPTS};