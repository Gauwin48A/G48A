const pool=require("../config/db");const logger=require("../utils/logger");const DB_QUERY_TIMEOUT_MS=Number.parseInt(process.env.DB_QUERY_TIMEOUT_MS,10)||1e4;const DEFAULT_AUTO_APPROVE_THRESHOLD=Number.parseFloat(process.env.KYC_AUTO_APPROVE_THRESHOLD||"0.9");const DEFAULT_MANUAL_REVIEW_THRESHOLD=Number.parseFloat(process.env.KYC_MANUAL_REVIEW_THRESHOLD||"0.7");let schemaReadyPromise=null;function runQuery(text,values=[]){return pool.query({text:text,values:values,query_timeout:DB_QUERY_TIMEOUT_MS})}function parseOptionalString(value){if(value===undefined||value===null)return null;const normalized=String(value).trim();return normalized.length?normalized:null}function maskAadhaar(aadhaarNumber){const digits=String(aadhaarNumber||"").replace(/\D/g,"");if(!digits)return null;return`XXXX-XXXX-${digits.slice(-4)}`}function maskPan(panNumber){const normalized=String(panNumber||"").trim().toUpperCase();if(!normalized)return null;return`XXXXX${normalized.slice(-4)}`}function normalizeAadhaar(aadhaarNumber){const digits=String(aadhaarNumber||"").replace(/\D/g,"");return digits||null}function normalizePan(panNumber){const normalized=String(panNumber||"").trim().toUpperCase().replace(/\s+/g,"");return normalized||null}function validateKycInputs(aadhaarNumber,panNumber,documents={}){const errors=[];const riskFlags=[];const normalizedAadhaar=normalizeAadhaar(aadhaarNumber);const normalizedPan=normalizePan(panNumber);const aadhaarValid=Boolean(normalizedAadhaar&&/^\d{12}$/.test(normalizedAadhaar));const panValid=Boolean(normalizedPan&&/^[A-Z]{5}[0-9]{4}[A-Z]$/.test(normalizedPan));if(!aadhaarValid){errors.push("Invalid Aadhaar format. Expected 12 digits.");riskFlags.push("invalid_aadhaar_format")}else if(/^(\d)\1{11}$/.test(normalizedAadhaar)){errors.push("Aadhaar number appears invalid.");riskFlags.push("aadhaar_repeating_digits")}if(!panValid){errors.push("Invalid PAN format. Expected ABCDE1234F.");riskFlags.push("invalid_pan_format")}if(!documents.front){errors.push("Front-side KYC document is missing.");riskFlags.push("missing_front_document")}if(!documents.back){riskFlags.push("missing_back_document")}return{normalizedAadhaar:normalizedAadhaar,normalizedPan:normalizedPan,aadhaarValid:aadhaarValid,panValid:panValid,errors:errors,riskFlags:riskFlags}}function buildOcrExtractionResult({normalizedAadhaar:normalizedAadhaar,normalizedPan:normalizedPan,documents:documents,validation:validation}){let confidence=.95;if(!documents.back)confidence-=.08;if(!validation.aadhaarValid)confidence-=.45;if(!validation.panValid)confidence-=.35;if(validation.riskFlags.includes("aadhaar_repeating_digits"))confidence-=.15;const boundedConfidence=Math.max(0,Math.min(.99,Number(confidence.toFixed(2))));return{provider:process.env.KYC_OCR_PROVIDER||"mock_ocr",extracted:{aadhaar_number:normalizedAadhaar?maskAadhaar(normalizedAadhaar):null,pan_number:normalizedPan?maskPan(normalizedPan):null,document_count:[documents.front,documents.back].filter(Boolean).length},confidence:boundedConfidence}}function decideKycRoute({validation:validation,ocrConfidence:ocrConfidence}){if(!validation.aadhaarValid||!validation.panValid){return{decision:"auto_rejected",reason:validation.errors.join(" ")}}if(ocrConfidence>=DEFAULT_AUTO_APPROVE_THRESHOLD&&validation.riskFlags.length===0){return{decision:"auto_approved",reason:"KYC validated automatically with high confidence."}}if(ocrConfidence>=DEFAULT_MANUAL_REVIEW_THRESHOLD){return{decision:"manual_review",reason:"Manual review required due to medium confidence or minor risk flags."}}return{decision:"auto_rejected",reason:"KYC confidence too low. Please resubmit clearer documents."}}async function ensureSchema(){if(!schemaReadyPromise){schemaReadyPromise=(async()=>{await runQuery(`\n                CREATE TABLE IF NOT EXISTS kyc_review_queue (\n                    queue_id BIGSERIAL PRIMARY KEY,\n                    user_id TEXT NOT NULL,\n                    aadhaar_number_masked TEXT,\n                    pan_number_masked TEXT,\n                    documents JSONB NOT NULL DEFAULT '{}'::jsonb,\n                    ocr_data JSONB NOT NULL DEFAULT '{}'::jsonb,\n                    confidence NUMERIC(5, 2) NOT NULL DEFAULT 0,\n                    risk_flags JSONB NOT NULL DEFAULT '[]'::jsonb,\n                    decision VARCHAR(32) NOT NULL,\n                    decision_reason TEXT,\n                    status VARCHAR(24) NOT NULL DEFAULT 'pending',\n                    reviewed_by TEXT,\n                    review_notes TEXT,\n                    reviewed_at TIMESTAMPTZ,\n                    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),\n                    processed_at TIMESTAMPTZ\n                )\n            `);await runQuery("CREATE INDEX IF NOT EXISTS idx_kyc_review_queue_status_created ON kyc_review_queue(status, created_at DESC)");await runQuery("CREATE INDEX IF NOT EXISTS idx_kyc_review_queue_user_created ON kyc_review_queue(user_id, created_at DESC)")})().catch(err=>{logger.warn("[KYC] Schema check failed; queue persistence disabled",{message:err.message});return false})}return schemaReadyPromise}async function applyDecisionToUser(userId,decision,reason=null){if(decision==="auto_approved"||decision==="manual_approved"){await runQuery(`\n                UPDATE users\n                SET aadhaar_status = 'VERIFIED',\n                    rejection_reason = NULL\n                WHERE user_id::text = $1\n            `,[String(userId)]);return"VERIFIED"}if(decision==="auto_rejected"||decision==="manual_rejected"){await runQuery(`\n                UPDATE users\n                SET aadhaar_status = 'REJECTED',\n                    rejection_reason = $2\n                WHERE user_id::text = $1\n            `,[String(userId),reason||"KYC verification failed. Please resubmit valid documents."]);return"REJECTED"}await runQuery(`\n            UPDATE users\n            SET aadhaar_status = 'PENDING',\n                rejection_reason = NULL\n            WHERE user_id::text = $1\n        `,[String(userId)]);return"PENDING"}async function sendKycNotification(userId,decision,reason){const payloadMap={auto_approved:{title:"KYC Verified",message:"Your KYC has been auto-approved successfully."},manual_review:{title:"KYC Under Review",message:"Your KYC is in manual review. You will be notified once it is completed."},auto_rejected:{title:"KYC Rejected",message:reason||"Your KYC could not be verified automatically. Please resubmit."},manual_approved:{title:"KYC Approved",message:"Your KYC has been approved by the review team."},manual_rejected:{title:"KYC Rejected",message:reason||"Your KYC was rejected by the review team. Please resubmit."}};const notification=payloadMap[decision];if(!notification)return;try{await runQuery(`\n                INSERT INTO notifications (user_id, type, title, message, created_at)\n                VALUES ($1, 'kyc_status', $2, $3, NOW())\n            `,[String(userId),notification.title,notification.message])}catch(err){logger.warn("[KYC] Failed to create KYC notification",{message:err.message,userId:userId})}}async function enqueueKycDecision({userId:userId,aadhaarMasked:aadhaarMasked,panMasked:panMasked,documents:documents,ocrData:ocrData,confidence:confidence,riskFlags:riskFlags,decision:decision,reason:reason}){const schemaReady=await ensureSchema();if(schemaReady===false){return{queueId:null,persisted:false}}try{const status=decision==="manual_review"?"pending":"processed";const result=await runQuery(`\n                INSERT INTO kyc_review_queue (\n                    user_id,\n                    aadhaar_number_masked,\n                    pan_number_masked,\n                    documents,\n                    ocr_data,\n                    confidence,\n                    risk_flags,\n                    decision,\n                    decision_reason,\n                    status,\n                    processed_at\n                )\n                VALUES ($1, $2, $3, $4::jsonb, $5::jsonb, $6, $7::jsonb, $8, $9, $10,\n                    CASE WHEN $10 = 'processed' THEN NOW() ELSE NULL END\n                )\n                RETURNING queue_id\n            `,[String(userId),aadhaarMasked,panMasked,JSON.stringify(documents||{}),JSON.stringify(ocrData||{}),confidence,JSON.stringify(riskFlags||[]),decision,reason,status]);return{queueId:result.rows[0]?.queue_id||null,persisted:true}}catch(err){logger.warn("[KYC] Failed to enqueue review queue row",{message:err.message,userId:userId});return{queueId:null,persisted:false}}}async function processKycSubmission({userId:userId,aadhaarNumber:aadhaarNumber,panNumber:panNumber,documents:documents}){const validation=validateKycInputs(aadhaarNumber,panNumber,documents||{});const ocrResult=buildOcrExtractionResult({normalizedAadhaar:validation.normalizedAadhaar,normalizedPan:validation.normalizedPan,documents:documents||{},validation:validation});const routing=decideKycRoute({validation:validation,ocrConfidence:ocrResult.confidence});const queueResult=await enqueueKycDecision({userId:userId,aadhaarMasked:maskAadhaar(validation.normalizedAadhaar),panMasked:maskPan(validation.normalizedPan),documents:documents,ocrData:ocrResult,confidence:ocrResult.confidence,riskFlags:validation.riskFlags,decision:routing.decision,reason:routing.reason});const userStatus=await applyDecisionToUser(userId,routing.decision,routing.reason);await sendKycNotification(userId,routing.decision,routing.reason);return{queue_id:queueResult.queueId,queue_persisted:queueResult.persisted,decision:routing.decision,decision_reason:routing.reason,user_status:userStatus,confidence:ocrResult.confidence,risk_flags:validation.riskFlags,validation_errors:validation.errors}}async function listReviewQueue({status:status="pending",page:page=1,limit:limit=50}){const schemaReady=await ensureSchema();if(schemaReady===false){return{queue:[],total:0,page:page,limit:limit,available:false}}const normalizedStatus=String(status||"pending").trim().toLowerCase();const safePage=Math.max(1,Number.parseInt(page,10)||1);const safeLimit=Math.min(200,Math.max(1,Number.parseInt(limit,10)||50));const offset=(safePage-1)*safeLimit;const values=[safeLimit,offset];let statusSql="";if(normalizedStatus!=="all"){values.unshift(normalizedStatus);statusSql="WHERE status = $1"}const limitPlaceholder=normalizedStatus==="all"?"$1":"$2";const offsetPlaceholder=normalizedStatus==="all"?"$2":"$3";const result=await runQuery(`\n            SELECT\n                COUNT(*) OVER()::int AS total_count,\n                queue_id,\n                user_id,\n                aadhaar_number_masked,\n                pan_number_masked,\n                confidence,\n                risk_flags,\n                decision,\n                decision_reason,\n                status,\n                reviewed_by,\n                review_notes,\n                reviewed_at,\n                created_at,\n                processed_at\n            FROM kyc_review_queue\n            ${statusSql}\n            ORDER BY created_at ASC\n            LIMIT ${limitPlaceholder} OFFSET ${offsetPlaceholder}\n        `,values);const total=result.rows.length?Number(result.rows[0].total_count)||0:0;const queue=result.rows.map(({total_count:total_count,...row})=>row);return{queue:queue,total:total,page:safePage,limit:safeLimit,available:true}}async function reviewQueueItem({queueId:queueId,reviewerId:reviewerId,decision:decision,notes:notes}){const schemaReady=await ensureSchema();if(schemaReady===false){return{available:false,error:"KYC review queue unavailable"}}const normalizedDecision=String(decision||"").trim().toLowerCase();if(!["approve","reject"].includes(normalizedDecision)){return{available:true,error:"Decision must be approve or reject"}}const mappedDecision=normalizedDecision==="approve"?"manual_approved":"manual_rejected";const reason=normalizedDecision==="reject"?parseOptionalString(notes)||"KYC rejected during manual review.":parseOptionalString(notes)||"KYC approved during manual review.";const client=await pool.connect();try{await client.query("BEGIN");const queueResult=await client.query(`\n                SELECT\n                    queue_id,\n                    user_id,\n                    status\n                FROM kyc_review_queue\n                WHERE queue_id = $1\n                FOR UPDATE\n            `,[queueId]);if(!queueResult.rows.length){await client.query("ROLLBACK");return{available:true,not_found:true}}const queueItem=queueResult.rows[0];const reviewNotes=parseOptionalString(notes);await client.query(`\n                UPDATE kyc_review_queue\n                SET decision = $1,\n                    decision_reason = $2,\n                    status = 'processed',\n                    reviewed_by = $3,\n                    review_notes = $4,\n                    reviewed_at = NOW(),\n                    processed_at = NOW()\n                WHERE queue_id = $5\n            `,[mappedDecision,reason,String(reviewerId),reviewNotes,queueId]);if(mappedDecision==="manual_approved"){await client.query(`\n                    UPDATE users\n                    SET aadhaar_status = 'VERIFIED',\n                        rejection_reason = NULL\n                    WHERE user_id::text = $1\n                `,[String(queueItem.user_id)])}else{await client.query(`\n                    UPDATE users\n                    SET aadhaar_status = 'REJECTED',\n                        rejection_reason = $2\n                    WHERE user_id::text = $1\n                `,[String(queueItem.user_id),reason])}await client.query("COMMIT");await sendKycNotification(queueItem.user_id,mappedDecision,reason);return{available:true,reviewed:true,queue_id:queueId,user_id:queueItem.user_id,decision:mappedDecision,reason:reason}}catch(err){await client.query("ROLLBACK");logger.error("[KYC] Manual review failed",err);return{available:true,error:"Failed to review KYC queue item"}}finally{client.release()}}module.exports={processKycSubmission:processKycSubmission,listReviewQueue:listReviewQueue,reviewQueueItem:reviewQueueItem};