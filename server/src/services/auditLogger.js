const pool=require("../config/db");const MAX_AUDIT_LOG_LIMIT=200;function parsePositiveInt(value,fallback,max=Number.MAX_SAFE_INTEGER){const parsed=Number.parseInt(value,10);if(!Number.isSafeInteger(parsed)||parsed<1)return fallback;return Math.min(parsed,max)}const AUDIT_ACTIONS={LOGIN_SUCCESS:"LOGIN_SUCCESS",LOGIN_FAILED:"LOGIN_FAILED",LOGOUT:"LOGOUT",PASSWORD_CHANGED:"PASSWORD_CHANGED",PASSWORD_RESET_REQUESTED:"PASSWORD_RESET_REQUESTED",PASSWORD_RESET_COMPLETED:"PASSWORD_RESET_COMPLETED",TWO_FACTOR_ENABLED:"TWO_FACTOR_ENABLED",TWO_FACTOR_DISABLED:"TWO_FACTOR_DISABLED",TWO_FACTOR_FAILED:"TWO_FACTOR_FAILED",ACCOUNT_CREATED:"ACCOUNT_CREATED",ACCOUNT_DELETED:"ACCOUNT_DELETED",PROFILE_UPDATED:"PROFILE_UPDATED",EMAIL_CHANGED:"EMAIL_CHANGED",SUSPICIOUS_ACTIVITY:"SUSPICIOUS_ACTIVITY",BRUTE_FORCE_DETECTED:"BRUTE_FORCE_DETECTED",NEW_DEVICE_LOGIN:"NEW_DEVICE_LOGIN",DATA_EXPORTED:"DATA_EXPORTED",SENSITIVE_DATA_ACCESSED:"SENSITIVE_DATA_ACCESSED"};const getClientIP=req=>req.headers["x-forwarded-for"]?.split(",")[0]?.trim()||req.headers["x-real-ip"]||req.socket?.remoteAddress||req.ip||"unknown";const getDeviceFingerprint=req=>{const userAgent=req.headers["user-agent"]||"";const acceptLang=req.headers["accept-language"]||"";const acceptEnc=req.headers["accept-encoding"]||"";const fingerprint=Buffer.from(`${userAgent}|${acceptLang}|${acceptEnc}`).toString("base64").substring(0,32);return fingerprint};const logAudit=async({userId:userId=null,action:action,req:req,details:details={}})=>{try{const ip=getClientIP(req);const userAgent=req.headers["user-agent"]||"unknown";const fingerprint=getDeviceFingerprint(req);const enrichedDetails={...details,deviceFingerprint:fingerprint,timestamp:(new Date).toISOString()};await pool.query(`INSERT INTO audit_logs (user_id, action, ip_address, user_agent, details) \n       VALUES ($1, $2, $3, $4, $5)`,[userId,action,ip,userAgent,JSON.stringify(enrichedDetails)]);if(["BRUTE_FORCE_DETECTED","SUSPICIOUS_ACTIVITY","LOGIN_FAILED"].includes(action)){console.warn(`[SECURITY AUDIT] ${action} - IP: ${ip} - User: ${userId||"anonymous"}`)}}catch(err){console.error("[AUDIT LOGGER] Failed to log:",err.message)}};const logSecurityEvent=async({eventType:eventType,userId:userId=null,ip:ip,severity:severity="MEDIUM",details:details={}})=>{try{await pool.query(`INSERT INTO security_events (event_type, user_id, ip_address, severity, details) \n       VALUES ($1, $2, $3, $4, $5)`,[eventType,userId,ip,severity,JSON.stringify(details)]);if(severity==="CRITICAL"){console.error(`[CRITICAL SECURITY EVENT] ${eventType} - IP: ${ip}`)}}catch(err){console.error("[SECURITY EVENT LOGGER] Failed:",err.message)}};const getUserAuditLogs=async(userId,limit=50)=>{try{const safeLimit=parsePositiveInt(limit,50,MAX_AUDIT_LOG_LIMIT);const result=await pool.query(`SELECT action, ip_address, user_agent, details, created_at \n       FROM audit_logs \n       WHERE user_id = $1 \n       ORDER BY created_at DESC \n       LIMIT $2`,[userId,safeLimit]);return result.rows}catch(err){console.error("[AUDIT LOGGER] Failed to fetch logs:",err.message);return[]}};module.exports={logAudit:logAudit,logSecurityEvent:logSecurityEvent,getUserAuditLogs:getUserAuditLogs,getClientIP:getClientIP,getDeviceFingerprint:getDeviceFingerprint,AUDIT_ACTIONS:AUDIT_ACTIONS};