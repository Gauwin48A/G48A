const logger=require("../utils/logger");let sentryClient=null;let initAttempted=false;function initErrorReporter(){if(initAttempted)return;initAttempted=true;const dsn=String(process.env.SENTRY_DSN||"").trim();if(!dsn){logger.info("[ErrorReporter] Sentry disabled (SENTRY_DSN not set).");return}try{const Sentry=require("@sentry/node");Sentry.init({dsn:dsn,environment:process.env.NODE_ENV||"development",tracesSampleRate:Number.parseFloat(process.env.SENTRY_TRACES_SAMPLE_RATE||"0.1")});sentryClient=Sentry;logger.info("[ErrorReporter] Sentry initialized.")}catch(err){logger.warn("[ErrorReporter] SENTRY_DSN is set but @sentry/node is not installed. Falling back to logs.")}}function isSentryEnabled(){return Boolean(sentryClient)}function captureException(error,context={}){if(sentryClient){sentryClient.captureException(error,{extra:context});return}logger.error("[ErrorReporter][exception]",error?.message||"Unknown error",context)}function captureClientError(payload={},context={}){const message=String(payload?.message||"Client runtime error").slice(0,500);if(sentryClient){sentryClient.captureMessage(message,{level:"error",extra:{...context,payload:payload}});return}logger.error("[ErrorReporter][client]",message,{...context,payload:payload})}module.exports={initErrorReporter:initErrorReporter,isSentryEnabled:isSentryEnabled,captureException:captureException,captureClientError:captureClientError};