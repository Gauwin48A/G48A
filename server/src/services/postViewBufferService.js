const pool=require("../config/db");const logger=require("../utils/logger");const DEFAULT_BATCH_LIMIT=Number.parseInt(process.env.BATCH_VIEW_MAX_IDS||"500",10);const DEFAULT_FLUSH_INTERVAL_MS=Number.parseInt(process.env.BATCH_VIEW_FLUSH_INTERVAL_MS||"250",10);const DEFAULT_MAX_BUFFERED_KEYS=Number.parseInt(process.env.BATCH_VIEW_MAX_BUFFERED_KEYS||"5000",10);const DB_QUERY_TIMEOUT_MS=Number.parseInt(process.env.DB_QUERY_TIMEOUT_MS||"10000",10);const UUID_RE=/^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i;const pendingCounts=new Map;let flushTimer=null;let flushInProgress=false;let droppedDueToBufferLimit=0;function runQuery(text,values=[]){return pool.query({text:text,values:values,query_timeout:DB_QUERY_TIMEOUT_MS})}function sanitizePostIds(postIds,limit=DEFAULT_BATCH_LIMIT){if(!Array.isArray(postIds)||postIds.length===0)return[];const out=[];const seen=new Set;for(const rawId of postIds){if(out.length>=limit)break;const id=String(rawId||"").trim().toLowerCase();if(!id||seen.has(id)||!UUID_RE.test(id))continue;seen.add(id);out.push(id)}return out}function getQueueStats(){return{bufferedPostCount:pendingCounts.size,flushInProgress:flushInProgress,flushScheduled:Boolean(flushTimer),droppedDueToBufferLimit:droppedDueToBufferLimit}}function mergeBackCounts(countMap){for(const[postId,increment]of countMap.entries()){const current=pendingCounts.get(postId)||0;if(pendingCounts.size>=DEFAULT_MAX_BUFFERED_KEYS&&!pendingCounts.has(postId)){droppedDueToBufferLimit+=1;continue}pendingCounts.set(postId,current+increment)}}async function flushCountMap(countMap){if(!countMap||countMap.size===0)return{updatedRows:0,flushedIds:0};const ids=[];const increments=[];for(const[postId,increment]of countMap.entries()){ids.push(postId);increments.push(Number(increment)||1)}const result=await runQuery(`\n            UPDATE posts p\n            SET views = COALESCE(p.views, 0) + v.increment\n            FROM (\n                SELECT * FROM UNNEST($1::uuid[], $2::int[])\n            ) AS v(post_id, increment)\n            WHERE p.post_id = v.post_id\n        `,[ids,increments]);return{updatedRows:result.rowCount||0,flushedIds:ids.length}}function clearFlushTimer(){if(flushTimer){clearTimeout(flushTimer);flushTimer=null}}function scheduleFlush(){if(flushTimer||flushInProgress||pendingCounts.size===0)return false;flushTimer=setTimeout(()=>{void flushNow()},DEFAULT_FLUSH_INTERVAL_MS);if(typeof flushTimer.unref==="function"){flushTimer.unref()}return true}async function flushNow(){if(flushInProgress)return{skipped:"already_flushing"};if(pendingCounts.size===0)return{updatedRows:0,flushedIds:0};flushInProgress=true;clearFlushTimer();const snapshot=new Map(pendingCounts);pendingCounts.clear();try{const result=await flushCountMap(snapshot);return{...result,failed:false}}catch(err){mergeBackCounts(snapshot);logger.warn("[PostViewBuffer] Flush failed; increments re-queued",{message:err.message,bufferedPostCount:pendingCounts.size});return{updatedRows:0,flushedIds:snapshot.size,failed:true,error:err.message}}finally{flushInProgress=false;if(pendingCounts.size>0){scheduleFlush()}}}async function enqueueBatchView(postIds,options={}){const syncMode=options.syncMode===true||process.env.BATCH_VIEW_SYNC_MODE==="true";const sanitized=sanitizePostIds(postIds,options.limit||DEFAULT_BATCH_LIMIT);if(sanitized.length===0){return{mode:syncMode?"sync":"async",queued:0,skipped:Array.isArray(postIds)?postIds.length:0,updated:0,flushScheduled:false}}if(syncMode){const countMap=new Map;for(const postId of sanitized){countMap.set(postId,(countMap.get(postId)||0)+1)}const flushResult=await flushCountMap(countMap);return{mode:"sync",queued:sanitized.length,skipped:Math.max(0,(Array.isArray(postIds)?postIds.length:sanitized.length)-sanitized.length),updated:flushResult.updatedRows,flushScheduled:false}}let queued=0;for(const postId of sanitized){if(pendingCounts.size>=DEFAULT_MAX_BUFFERED_KEYS&&!pendingCounts.has(postId)){droppedDueToBufferLimit+=1;continue}pendingCounts.set(postId,(pendingCounts.get(postId)||0)+1);queued+=1}const flushScheduled=scheduleFlush();return{mode:"async",queued:queued,skipped:Math.max(0,(Array.isArray(postIds)?postIds.length:queued)-queued),updated:0,flushScheduled:flushScheduled}}function resetForTests(){clearFlushTimer();flushInProgress=false;droppedDueToBufferLimit=0;pendingCounts.clear()}module.exports={enqueueBatchView:enqueueBatchView,flushNow:flushNow,getQueueStats:getQueueStats,sanitizePostIds:sanitizePostIds,resetForTests:resetForTests};