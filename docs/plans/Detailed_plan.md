Use below all  and develop a plan in phases/sprints and implemet everything carefully and achieve 10/10 execution is required:

main concern and focus is on myhome page and  saledone, saleundone pages which are very bad not web app parity matching  http://localhost:8081/

in we bapp login with demo credentails and  analyze those pages and same 100% develop those pages here as well.

saledone, sale undone in our android app they were completely different when compared with web app.

and myhome  erroro not laoding.

and for-you page is also same not working 
allposts  no comapre optoion the feature  which we had in  web app and  for post cards the  like share etc  not available that icons etc

many gaps we had when compared with web app we need to resolve all those  for almost all pages.

# FINAL MASTER IMPLEMENTATION PROMPT

# COMPLETE WEB-APP PARITY REBUILD FOR FAILED ANDROID MODULES

# ======================================================================
CRITICAL PROJECT FAILURE STATUS
The Android application is STILL failing to achieve true parity with the web application.

Even after multiple implementation iterations, major modules remain:

- broken
- partially implemented
- visually inconsistent
- semantically incorrect
- structurally different
- functionally incomplete
- disconnected from actual web app behavior

This project can NO LONGER continue with:

- approximate implementations
- guessed workflows
- invented UI/UX
- fake parity
- placeholder logic
- random feature assumptions

The current Android app still feels like:

- a partially rebuilt experimental app
- disconnected from the web ecosystem
- inconsistent across pages
- unstable in navigation and rendering
- visually fragmented

instead of:

ONE unified, production-grade, mobile-first extension of the MHUB web platform.

# ======================================================================
MASTER OBJECTIVE
Transform the Android app into:

- a true mobile extension of the MHUB web app
- semantically identical to the web app
- visually aligned with the web app
- functionally identical to the web app
- stable and production-ready
- mobile-optimized while preserving web parity
- bug-free
- vulnerability-free
- fully tested
- feature complete

Target Quality:

- 100/100 web parity
- 10/10 implementation rating
- zero fake implementations
- zero invented workflows
- zero broken states
- zero crashes
- zero UI drift

# ======================================================================
MANDATORY SOURCE OF TRUTH
WEB APP SOURCE CODE PATH:

C:\Users\laksh\GITHUB\MHUB\Mhub

WEB APP LOCALHOST:

[http://localhost:8081/](http://localhost:8081/)

These are now the ONLY VALID REFERENCES.

DO NOT:

- guess functionality
- redesign independently
- invent routes
- invent APIs
- invent workflows
- invent page structures

Everything MUST come directly from:

- localhost behavior
- actual web app source code
- actual API logic
- actual component structure

# ======================================================================
MANDATORY IMPLEMENTATION PROCESS
FOR EVERY PAGE:

STEP 1 — LOCALHOST ANALYSIS

- Open page in localhost
- Login with valid user account
- Analyze:page structure
- UI hierarchy
- navigation flow
- interactions
- API behavior
- filters
- dropdowns
- loading states
- error states
- empty states
- ownership logic
- permissions
- button behavior

STEP 2 — SOURCE CODE ANALYSIS
Inspect actual web app code:

- routes
- components
- hooks
- services
- APIs
- stores/state
- payload structures
- validations
- conditions
- rendering logic
- auth checks

STEP 3 — SCREENSHOT CAPTURE
Capture:

- full-page screenshots
- modal screenshots
- dropdown screenshots
- loading states
- responsive/mobile layouts

STEP 4 — ANDROID COMPARISON
Compare Android implementation side-by-side with web app.

STEP 5 — REBUILD IF REQUIRED
If parity is below 100%:

- DELETE current Android implementation
- REBUILD from scratch using web app as reference

# ======================================================================
CRITICAL FAILED MODULES — HIGHEST PRIORITY
The following pages are STILL NOT MATCHING WEB APP:

- Feed
- My Home
- For You
- Feedback
- Sale Done
- Sale Undone
- Complaints (partial parity only)

These pages currently contain:

- incorrect workflows
- wrong semantics
- incorrect layouts
- missing logic
- fake functionality
- loading failures
- crashes
- API mismatches
- empty dropdowns
- invalid states

======================================================================

1. FEED PAGE — COMPLETE REBUILD
======================================================================

CURRENT FAILURE STATUS:

- not loading correctly
- incorrect content structure
- not matching web app
- wrong UI hierarchy
- incorrect API integration

CORRECT WEB APP SEMANTICS:
Feed =

- news
- knowledge sharing
- discussion content
- descriptive social posts

Feed is NOT:

- marketplace listing page
- generic product feed
- image-only layout

MANDATORY IMPLEMENTATION:
Clone EXACTLY from web app:

- feed layout
- card design
- typography hierarchy
- expandable descriptions
- engagement interactions
- pagination behavior
- loading states
- empty states
- responsive layout

MANDATORY RULE:
If Android Feed differs from web Feed:
DELETE and rebuild completely.

TARGET:
10/10 parity score.

# ======================================================================
2. MY HOME PAGE — COMPLETE REBUILD
CURRENT FAILURE STATUS:

- not loading
- incorrect data rendering
- wrong page purpose
- dashboard-like behavior

CORRECT WEB APP SEMANTICS:
My Home =
current user’s own listings/posts ONLY.

NOT:

- dashboard
- feed clone
- analytics page

MANDATORY IMPLEMENTATION:

- ownership-based filtering
- user-owned listings only
- edit/delete functionality
- listing management actions
- exact card layout from web app
- same API logic
- same loading states
- same empty states

MANDATORY:
Analyze ownership APIs and filtering logic from source code.

TARGET:
10/10 parity score.

# ======================================================================
3. FOR YOU PAGE — COMPLETE REBUILD
CURRENT FAILURE STATUS:

- duplicate of AllPosts
- recommendation logic missing
- previously crashing
- fake implementation

MANDATORY:
Analyze actual recommendation logic in web app.

ForYou MUST:

- NOT duplicate AllPosts
- use recommendation/personalized flows
- preserve category relevance
- show personalized listing behavior

IMPLEMENT:

- recommendation engine parity
- personalized filters
- sorting parity
- recommendation cards
- API parity
- state parity

MANDATORY:
If current implementation differs from web app:
DELETE and rebuild.

TARGET:
10/10 parity score.

# ======================================================================
4. SALE DONE PAGE — COMPLETE REBUILD
CURRENT FAILURE STATUS:

- wrong workflow
- fake “sold history” logic
- incorrect buyer confirmation flow
- mismatched UI
- invalid dropdown behavior

CRITICAL ISSUE:
Android implementation invented features not present in web app.

MANDATORY:
Inspect actual web app:

- dropdown options
- validations
- workflow steps
- permissions
- payload structure
- submission logic

REMOVE:

- fake sold history
- invented buyer confirmation flows
- non-web functionality

IMPLEMENT:

- exact web workflow
- exact dialogs/modals
- exact API payloads
- exact validations
- exact UI hierarchy

TARGET:
10/10 parity score.

# ======================================================================
5. SALE UNDONE PAGE — COMPLETE REBUILD
CURRENT FAILURE STATUS:

- reason dropdown empty
- incorrect API integration
- wrong form behavior
- invalid workflows

MANDATORY:
Inspect:

- dropdown APIs
- payloads
- reason values
- validation rules
- submission flows

IMPLEMENT:

- proper dropdown population
- exact reason options
- API parity
- exact validations
- proper form handling

FAIL CONDITIONS:

- empty dropdown
- missing reasons
- invalid payloads
- incorrect routes

TARGET:
10/10 parity score.

# ======================================================================
6. FEEDBACK PAGE — COMPLETE REBUILD
CURRENT FAILURE STATUS:

- incorrect forms
- mismatched UI
- wrong hierarchy
- incomplete functionality

MANDATORY:
Clone directly from web app:

- layout
- fields
- validations
- CTA hierarchy
- submission flows
- success/error handling

NO guessed forms allowed.

TARGET:
10/10 parity score.

# ======================================================================
7. COMPLAINTS PAGE — PARITY REFINEMENT
CURRENT STATUS:
~50% parity achieved.

STILL REQUIRED:

- exact spacing
- exact hierarchy
- API parity
- responsive parity
- loading state parity
- empty state parity
- interaction parity

DO NOT mark complete yet.

TARGET:
10/10 parity score.

# ======================================================================
MANDATORY PAGE-BY-PAGE GAP ANALYSIS
FOR EVERY PAGE PROVIDE:

| Page | Web Parity % | UI Match % | Logic Match % | API Match % | Missing Features | Current Bugs | Final Status |

DO NOT mark:
“Implemented”

UNTIL:
100% parity achieved.

# ======================================================================
ZERO BUG / ZERO ERROR / ZERO VULNERABILITY REQUIREMENTS
ALL PAGES MUST HAVE:

- zero crashes
- zero blank screens
- zero API failures
- zero stale states
- zero lifecycle bugs
- zero dropdown failures
- zero invalid payloads
- zero navigation corruption
- zero memory leaks
- zero console errors

SECURITY REQUIREMENTS:

- secure token handling
- sanitized payloads
- proper auth validation
- protected routes
- safe local storage
- API validation
- no unauthorized access

# ======================================================================
MANDATORY TESTING REQUIREMENTS
FOR EVERY PAGE TEST:

- loading flow
- error flow
- empty state
- API state
- auth state
- guest state
- navigation flow
- back-stack behavior
- refresh behavior
- dropdown behavior
- modal behavior
- responsive behavior
- localization behavior

MANDATORY:
Regression testing after EVERY fix.

# ======================================================================
STRICT REBUILD RULE
IF ANY PAGE:

- differs visually from web app
- differs semantically from web app
- differs functionally from web app
- contains fake logic
- contains guessed workflows
- crashes
- fails loading

THEN:
DELETE Android implementation
AND
REBUILD FROM SCRATCH USING WEB APP.

# ======================================================================
FINAL ACCEPTANCE CRITERIA
DO NOT MARK COMPLETE IF:

- Feed differs from web app
- My Home differs from web app
- ForYou differs from web app
- SaleDone differs from web app
- SaleUndone differs from web app
- Feedback differs from web app
- Complaints differs from web app

OR IF:

- any dropdown empty
- any page crashes
- any API mismatch exists
- any UI drift exists
- any feature invented independently

# ======================================================================
FINAL GOAL
The Android application must become:

- a native mobile extension of MHUB web platform
- visually identical in experience
- semantically identical
- API identical
- workflow identical
- production stable
- premium quality

with:

- 100% parity
- 10/10 implementation quality
- zero fake implementations
- zero placeholder logic
- zero invented workflows
- zero bugs
- zero crashes
- zero inconsistencies

The final result must feel like:
the actual MHUB web platform running natively on Android,
NOT a partially recreated approximation.

# CRITICAL WEB PARITY IMPLEMENTATION DIRECTIVE

# MYHOME, SALEDONE, SALEUNDONE & CORE PAGE RECONSTRUCTION
The PRIMARY focus area now is:

- MyHome page
- SaleDone page
- SaleUndone page

These pages are currently the WORST implemented modules in the Android app and are still NOT matching the web application hosted at:

[http://localhost:8081/](http://localhost:8081/)

The implementation quality for these pages is extremely poor and nowhere near actual web parity.

# ======================================================================
MANDATORY WEB APP ANALYSIS
Login into the web app using demo credentials and deeply analyze:

- page structure
- UI hierarchy
- navigation flow
- APIs
- filters
- dropdown behavior
- ownership logic
- form validations
- loading states
- empty states
- responsive/mobile layout
- actions and workflows

Then reproduce the EXACT same implementation in Android.

NO assumptions.
NO approximations.
NO Android-specific redesigns.

The web app is the ONLY source of truth.

======================================================================

1. MYHOME PAGE — COMPLETE FAILURE
======================================================================

Current Android Status:

- not loading
- errors occurring
- broken state handling
- incorrect implementation
- not matching web app semantics

MANDATORY REQUIREMENTS:

- analyze MyHome page directly from localhost
- inspect source code and APIs
- reproduce exact ownership logic
- reproduce exact listing rendering
- reproduce exact actions and flows

MyHome should ONLY show:

- current user’s own listings/posts

NOT:

- dashboard clone
- feed clone
- generic profile page

MANDATORY:
If current implementation is structurally wrong:
DELETE and rebuild from scratch using web app reference.

# ======================================================================
2. SALEDONE PAGE — MAJOR WEB PARITY FAILURE
Current Android Status:

- completely different from web app
- wrong workflows
- invented features
- incorrect forms
- incorrect structure

CRITICAL ISSUE:
Android implementation introduced functionality not present in the web app.

Example:

- Sold History
- fake Buyer Confirm flow

These do NOT exist in the web app implementation.

REMOVE all invented functionality immediately.

MANDATORY:
Analyze actual SaleDone flow from localhost and source code.

Rebuild EXACTLY:

- UI hierarchy
- forms
- dropdowns
- validations
- APIs
- actions
- permissions
- workflows

Target:
100% web parity.

# ======================================================================
3. SALEUNDONE PAGE — MAJOR WEB PARITY FAILURE
Current Android Status:

- reason dropdown empty
- incorrect APIs
- invalid workflow
- broken form logic

MANDATORY:
Inspect actual SaleUndone implementation from:

- localhost
- source code
- API responses

Then rebuild:

- dropdown population
- validations
- submission logic
- API payloads
- UI hierarchy
- modals/dialogs

The Android version must behave EXACTLY like the web app.

# ======================================================================
4. FOR-YOU PAGE — STILL NOT WORKING
Current Android Status:

- broken
- incorrect rendering
- not matching web app
- recommendation logic missing

MANDATORY:
Analyze actual recommendation/personalization flow from web app.

ForYou must:

- NOT duplicate AllPosts
- use recommendation logic
- use personalized rendering
- match web app behavior exactly

# ======================================================================
5. ALLPOSTS PAGE — MISSING CORE FEATURES
Current Android Issues:

- compare option missing
- compare workflow missing
- like/share icons missing
- card interactions incomplete
- post cards not matching web app

MANDATORY:
Analyze AllPosts from localhost and source code.

Implement:

- compare feature
- compare selection workflow
- like/share actions
- post interaction icons
- exact card design parity
- exact filters and sorting
- exact subcategory behavior

The Android AllPosts page must fully match:

- look
- feel
- interactions
- workflows
- APIs
- features

# ======================================================================
GLOBAL WEB PARITY REQUIREMENT
There are still MANY gaps across almost all pages when compared with the web app.

The implementation team MUST:

- compare EVERY page side-by-side with localhost
- capture screenshots
- inspect APIs
- inspect components
- inspect workflows
- inspect navigation behavior
- inspect state handling

DO NOT:

- create independent Android interpretations
- create approximate flows
- invent missing functionality

# ======================================================================
MANDATORY RULE
If parity is not 100%:
DO NOT mark implemented.

Continue iterating until:

- UI parity achieved
- workflow parity achieved
- API parity achieved
- navigation parity achieved
- feature parity achieved

# ======================================================================
FINAL TARGET
The Android app must feel like:

the ACTUAL MHUB web platform running natively on Android,

NOT:
a loosely recreated version with guessed functionality.

Target:

- 100% parity
- 10/10 implementation quality
- zero invented logic
- zero missing functionality
- zero crashes
- zero loading issues
- zero API mismatches
- zero UI drift
- zero broken flows.

- The previously reported issues still remain unresolved across multiple pages with little to no visible improvement.
- Pages such as **My Home**, **Sale Done**, and **Sale Undone** are still showing errors and are not matching the improved center-aligned designs implemented in the web app. Android implementation lacks proper parity with the web version.
- **Feed**, **For You**, and **My Home** pages are still extremely underdeveloped and do not reflect the discussed requirements. It is better to completely delete and rebuild these pages from scratch using the web app as the reference source.
- **Feedback** and **Complaints** pages have partially improved, but they still require stronger parity with the web app.
- **Sale Done** and **Sale Undone** pages do not match the web app in style, structure, UI, or design. Current implementation is far from acceptable. These pages should be recreated entirely from scratch if necessary.
- **Sell** and **Plans** pages were not visually upgraded or enhanced properly. They require better UI/UX improvements and stronger visual quality.
- The **All Posts** page still lacks parity with the web app and requires major enhancements:Better UI/UX and more polished design
- Realistic sample posts and demo data
- Improved post cards
- Better feature implementation
- More user-friendly layout
- Proper comparison functionality
- Matching features available in the web app

- Using the provided `.md` files, implement all functionality completely and ensure the **language feature** works exactly like the web app using proper localization libraries such as `i18n` or equivalent.
- Reference the web application running at:`http://localhost:8081/`
- Use demo login credentials
- Analyze the exact page structure, styling, and flows
- Replicate the same pages and behavior in Android

- Use the web app codebase located at:`C:\Users\laksh\GITHUB\MHUB\Mhub`
- Achieve full Android parity with the web app implementation.

- Several Android pages such as **My Home**, **Sale Done**, **Sale Undone**, **Feedback**, etc. appear to have been independently designed instead of following the web app reference. Remove these implementations and recreate them properly based on the web app.
- In **All Posts**, implement:Proper subcategories
- Meaningful icons
- Professional app-like UI/UX
- Better visual hierarchy and design quality

- Improve the implementation quality overall without breaking or degrading existing functionality.
- The **For You** page should function as a filtered version of **All Posts**:Based on user preferences selected in the profile page
- Same UI/layout as All Posts
- Only filtered content differs

- Post cards in **All Posts** must match the **Feed** page design:Same buttons
- Same layout
- Same look & feel
- Same interactions such as Share, View, etc.

- Features available in the web app but missing in Android must be implemented properly, including:Compare functionality
- Additional post interactions
- Related features wherever applicable

- Compare **My Home**, **Sale Done**, **Sale Undone**, **Feedback**, and other pages directly against the web app:Capture screenshots
- Verify parity
- Improve UI beyond the current version if needed

- If a user is already logged in, navigating to another page should never ask for login again. Such authentication/session handling issues must be fixed completely.
- **Plans**, **Sell**, and **Add Post** pages must:Match the web app functionality
- Match the web app styling
- Have improved mobile-friendly UI/UX
- Be visually attractive and user-friendly

- Verify whether all previously mentioned requirements were actually implemented.If completed, provide implementation ratings
- If incomplete, implement them first and then provide ratings for each section

- Re-verify all **27 phases/modules/features** and ensure nothing is left incomplete.
- Do not launch the Android emulator until all updated pages achieve near-perfect parity with the web app.Repeatedly refine and improve until quality reaches a 10/10 standard

- Pages such as:My Offers
- My Reviews
- Help & Support
- Chat
- Public Wall
were created in Android despite not existing in the web app.
- Analyze whether these are truly useful
- Remove redundant pages if unnecessary

- The 4 categories on the home page represent separate app experiences for users.These categories should remain isolated from each other
- Only shared/common modules are:Profile
- Rewards
- Plans
- Sell
- KYC

- All other functional pages are category-specific

- Category-specific pages include:All Posts
- My Posts
- Feed
- For You
- My Feed
- My Home
- Sale Done
- Sale Undone
- Feedback & Complaints
- Subcategories
These should not mix content across categories.

- First ensure the exact web app functionality is implemented correctly before attempting custom Android improvements.
- `+` button flow should work as follows:If the user has an active plan → directly open Sell page
- If no active plan → open Plans page
- User should only access Sell after purchasing an active plan

- Current `+` button flow has multiple issues:Incorrect subcategory display
- Unexpected language rendering
- Internal server errors
- Navigation inconsistencies
All such issues must be fixed comprehensively.

- KYC verification flow should work properly:New users clicking `+` should first be redirected to complete KYC
- After successful KYC → return user to All Posts
- Then clicking `+` should show Plans page
- After plan activation → Sell page becomes accessible
- If plan expires → clicking `+` should again redirect to Plans page

- The entire app requires proper real-time functional flows and production-level logic handling.
- A single active plan should work across all 4 categories.
- Common/shared modules across categories:Rewards
- Profile
- KYC
- Plans

- All other core pages and workflows must remain isolated and category-specific rather than globally shared.# FINAL MASTER IMPLEMENTATION PROMPT

# COMPLETE WEB-APP PARITY REBUILD FOR FAILED ANDROID MODULES

# ======================================================================
CRITICAL PROJECT FAILURE STATUS
The Android application is STILL failing to achieve true parity with the web application.

Even after multiple implementation iterations, major modules remain:

- broken
- partially implemented
- visually inconsistent
- semantically incorrect
- structurally different
- functionally incomplete
- disconnected from actual web app behavior

This project can NO LONGER continue with:

- approximate implementations
- guessed workflows
- invented UI/UX
- fake parity
- placeholder logic
- random feature assumptions

The current Android app still feels like:

- a partially rebuilt experimental app
- disconnected from the web ecosystem
- inconsistent across pages
- unstable in navigation and rendering
- visually fragmented

instead of:

ONE unified, production-grade, mobile-first extension of the MHUB web platform.

# ======================================================================
MASTER OBJECTIVE
Transform the Android app into:

- a true mobile extension of the MHUB web app
- semantically identical to the web app
- visually aligned with the web app
- functionally identical to the web app
- stable and production-ready
- mobile-optimized while preserving web parity
- bug-free
- vulnerability-free
- fully tested
- feature complete

Target Quality:

- 100/100 web parity
- 10/10 implementation rating
- zero fake implementations
- zero invented workflows
- zero broken states
- zero crashes
- zero UI drift

# ======================================================================
MANDATORY SOURCE OF TRUTH
WEB APP SOURCE CODE PATH:

C:\Users\laksh\GITHUB\MHUB\Mhub

WEB APP LOCALHOST:

[http://localhost:8081/](http://localhost:8081/)

These are now the ONLY VALID REFERENCES.

DO NOT:

- guess functionality
- redesign independently
- invent routes
- invent APIs
- invent workflows
- invent page structures

Everything MUST come directly from:

- localhost behavior
- actual web app source code
- actual API logic
- actual component structure

# ======================================================================
MANDATORY IMPLEMENTATION PROCESS
FOR EVERY PAGE:

STEP 1 — LOCALHOST ANALYSIS

- Open page in localhost
- Login with valid user account
- Analyze:page structure
- UI hierarchy
- navigation flow
- interactions
- API behavior
- filters
- dropdowns
- loading states
- error states
- empty states
- ownership logic
- permissions
- button behavior

STEP 2 — SOURCE CODE ANALYSIS
Inspect actual web app code:

- routes
- components
- hooks
- services
- APIs
- stores/state
- payload structures
- validations
- conditions
- rendering logic
- auth checks

STEP 3 — SCREENSHOT CAPTURE
Capture:

- full-page screenshots
- modal screenshots
- dropdown screenshots
- loading states
- responsive/mobile layouts

STEP 4 — ANDROID COMPARISON
Compare Android implementation side-by-side with web app.

STEP 5 — REBUILD IF REQUIRED
If parity is below 100%:

- DELETE current Android implementation
- REBUILD from scratch using web app as reference

# ======================================================================
CRITICAL FAILED MODULES — HIGHEST PRIORITY
The following pages are STILL NOT MATCHING WEB APP:

- Feed
- My Home
- For You
- Feedback
- Sale Done
- Sale Undone
- Complaints (partial parity only)

These pages currently contain:

- incorrect workflows
- wrong semantics
- incorrect layouts
- missing logic
- fake functionality
- loading failures
- crashes
- API mismatches
- empty dropdowns
- invalid states

======================================================================

1. FEED PAGE — COMPLETE REBUILD
======================================================================

CURRENT FAILURE STATUS:

- not loading correctly
- incorrect content structure
- not matching web app
- wrong UI hierarchy
- incorrect API integration

CORRECT WEB APP SEMANTICS:
Feed =

- news
- knowledge sharing
- discussion content
- descriptive social posts

Feed is NOT:

- marketplace listing page
- generic product feed
- image-only layout

MANDATORY IMPLEMENTATION:
Clone EXACTLY from web app:

- feed layout
- card design
- typography hierarchy
- expandable descriptions
- engagement interactions
- pagination behavior
- loading states
- empty states
- responsive layout

MANDATORY RULE:
If Android Feed differs from web Feed:
DELETE and rebuild completely.

TARGET:
10/10 parity score.

# ======================================================================
2. MY HOME PAGE — COMPLETE REBUILD
CURRENT FAILURE STATUS:

- not loading
- incorrect data rendering
- wrong page purpose
- dashboard-like behavior

CORRECT WEB APP SEMANTICS:
My Home =
current user’s own listings/posts ONLY.

NOT:

- dashboard
- feed clone
- analytics page

MANDATORY IMPLEMENTATION:

- ownership-based filtering
- user-owned listings only
- edit/delete functionality
- listing management actions
- exact card layout from web app
- same API logic
- same loading states
- same empty states

MANDATORY:
Analyze ownership APIs and filtering logic from source code.

TARGET:
10/10 parity score.

# ======================================================================
3. FOR YOU PAGE — COMPLETE REBUILD
CURRENT FAILURE STATUS:

- duplicate of AllPosts
- recommendation logic missing
- previously crashing
- fake implementation

MANDATORY:
Analyze actual recommendation logic in web app.

ForYou MUST:

- NOT duplicate AllPosts
- use recommendation/personalized flows
- preserve category relevance
- show personalized listing behavior

IMPLEMENT:

- recommendation engine parity
- personalized filters
- sorting parity
- recommendation cards
- API parity
- state parity

MANDATORY:
If current implementation differs from web app:
DELETE and rebuild.

TARGET:
10/10 parity score.

# ======================================================================
4. SALE DONE PAGE — COMPLETE REBUILD
CURRENT FAILURE STATUS:

- wrong workflow
- fake “sold history” logic
- incorrect buyer confirmation flow
- mismatched UI
- invalid dropdown behavior

CRITICAL ISSUE:
Android implementation invented features not present in web app.

MANDATORY:
Inspect actual web app:

- dropdown options
- validations
- workflow steps
- permissions
- payload structure
- submission logic

REMOVE:

- fake sold history
- invented buyer confirmation flows
- non-web functionality

IMPLEMENT:

- exact web workflow
- exact dialogs/modals
- exact API payloads
- exact validations
- exact UI hierarchy

TARGET:
10/10 parity score.

# ======================================================================
5. SALE UNDONE PAGE — COMPLETE REBUILD
CURRENT FAILURE STATUS:

- reason dropdown empty
- incorrect API integration
- wrong form behavior
- invalid workflows

MANDATORY:
Inspect:

- dropdown APIs
- payloads
- reason values
- validation rules
- submission flows

IMPLEMENT:

- proper dropdown population
- exact reason options
- API parity
- exact validations
- proper form handling

FAIL CONDITIONS:

- empty dropdown
- missing reasons
- invalid payloads
- incorrect routes

TARGET:
10/10 parity score.

# ======================================================================
6. FEEDBACK PAGE — COMPLETE REBUILD
CURRENT FAILURE STATUS:

- incorrect forms
- mismatched UI
- wrong hierarchy
- incomplete functionality

MANDATORY:
Clone directly from web app:

- layout
- fields
- validations
- CTA hierarchy
- submission flows
- success/error handling

NO guessed forms allowed.

TARGET:
10/10 parity score.

# ======================================================================
7. COMPLAINTS PAGE — PARITY REFINEMENT
CURRENT STATUS:
~50% parity achieved.

STILL REQUIRED:

- exact spacing
- exact hierarchy
- API parity
- responsive parity
- loading state parity
- empty state parity
- interaction parity

DO NOT mark complete yet.

TARGET:
10/10 parity score.

# ======================================================================
MANDATORY PAGE-BY-PAGE GAP ANALYSIS
FOR EVERY PAGE PROVIDE:

| Page | Web Parity % | UI Match % | Logic Match % | API Match % | Missing Features | Current Bugs | Final Status |

DO NOT mark:
“Implemented”

UNTIL:
100% parity achieved.

# ======================================================================
ZERO BUG / ZERO ERROR / ZERO VULNERABILITY REQUIREMENTS
ALL PAGES MUST HAVE:

- zero crashes
- zero blank screens
- zero API failures
- zero stale states
- zero lifecycle bugs
- zero dropdown failures
- zero invalid payloads
- zero navigation corruption
- zero memory leaks
- zero console errors

SECURITY REQUIREMENTS:

- secure token handling
- sanitized payloads
- proper auth validation
- protected routes
- safe local storage
- API validation
- no unauthorized access

# ======================================================================
MANDATORY TESTING REQUIREMENTS
FOR EVERY PAGE TEST:

- loading flow
- error flow
- empty state
- API state
- auth state
- guest state
- navigation flow
- back-stack behavior
- refresh behavior
- dropdown behavior
- modal behavior
- responsive behavior
- localization behavior

MANDATORY:
Regression testing after EVERY fix.

# ======================================================================
STRICT REBUILD RULE
IF ANY PAGE:

- differs visually from web app
- differs semantically from web app
- differs functionally from web app
- contains fake logic
- contains guessed workflows
- crashes
- fails loading

THEN:
DELETE Android implementation
AND
REBUILD FROM SCRATCH USING WEB APP.

# ======================================================================
FINAL ACCEPTANCE CRITERIA
DO NOT MARK COMPLETE IF:

- Feed differs from web app
- My Home differs from web app
- ForYou differs from web app
- SaleDone differs from web app
- SaleUndone differs from web app
- Feedback differs from web app
- Complaints differs from web app

OR IF:

- any dropdown empty
- any page crashes
- any API mismatch exists
- any UI drift exists
- any feature invented independently

# ======================================================================
FINAL GOAL
The Android application must become:

- a native mobile extension of MHUB web platform
- visually identical in experience
- semantically identical
- API identical
- workflow identical
- production stable
- premium quality

with:

- 100% parity
- 10/10 implementation quality
- zero fake implementations
- zero placeholder logic
- zero invented workflows
- zero bugs
- zero crashes
- zero inconsistencies

The final result must feel like:
the actual MHUB web platform running natively on Android,
NOT a partially recreated approximation.# FINAL CRITICAL IMPLEMENTATION DIRECTIVE

# COMPLETE WEB-APP PARITY RECONSTRUCTION FOR FAILED ANDROID MODULES

# ======================================================================
CRITICAL FAILURE STATUS
The Android implementation is STILL NOT achieving real web-app parity.

Even after multiple iterations, major modules are:

- broken
- partially implemented
- semantically incorrect
- visually different
- structurally inconsistent
- non-functional
- disconnected from actual web app behavior

This is NOT acceptable anymore.

# ======================================================================
ROOT CAUSE IDENTIFIED
The team is still:

- NOT deeply analyzing the actual web app
- NOT using localhost correctly
- NOT reading the actual source code flows
- guessing implementations
- building approximate versions
- creating invented UI/logic
- partially cloning pages instead of reproducing behavior

The result:
Fake parity instead of real parity.

# ======================================================================
MANDATORY SOURCE OF TRUTH
WEB APP SOURCE CODE PATH:

C:\Users\laksh\GITHUB\MHUB\Mhub

LOCALHOST:

[http://localhost:8081/](http://localhost:8081/)

These are now the ONLY VALID SOURCES.

NO guessing allowed anymore.

# ======================================================================
MANDATORY IMPLEMENTATION STRATEGY
FOR EVERY FAILED PAGE:

1. Open page in localhost
2. Login using demo credentials
3. Capture:screenshots
4. navigation flow
5. API behavior
6. dropdown behavior
7. state behavior
8. permissions
9. filters
10. actions
11. loading states
12. empty states
13. error states
14. Then inspect:component hierarchy
15. API calls
16. business logic
17. hooks/state management
18. routing
19. conditional rendering
20. payload handling
21. validation rules
22. ONLY AFTER COMPLETE UNDERSTANDING:
rebuild Android implementation.

# ======================================================================
CRITICAL FAILED PAGES — HIGHEST PRIORITY
The following pages are STILL NOT MATCHING WEB APP EVEN 1%:

- Feed
- My Home
- For You
- Feedback
- Sale Done
- Sale Undone

Some pages are:

- not loading
- crashing
- blank
- partially working
- using wrong semantics
- using fake UI
- using invented business logic

# ======================================================================
PAGE-BY-PAGE MANDATORY FIX REQUIREMENTS
######################################################################

1. FEED PAGE — COMPLETE REBUILD
######################################################################

CURRENT FAILURE:

- not loading properly
- incorrect content type
- not matching web app
- incorrect cards
- wrong structure
- incorrect API usage
- likely crashing/state issues

---

## MANDATORY WEB PARITY REQUIREMENTS
Feed is:

- news
- knowledge
- descriptive sharing
- discussion content

Feed is NOT:

- marketplace listing page
- image-only listing system

---
IMPLEMENT EXACTLY FROM WEB APP:

- feed layout
- feed cards
- post rendering
- text hierarchy
- engagement actions
- expandable descriptions
- spacing
- API response handling
- pagination
- empty states
- loading states

---
MANDATORY:

Compare:
Web Feed vs Android Feed side-by-side.

IF NOT MATCHING:
DELETE Android implementation and rebuild from scratch.

---
TARGET SCORE:
10/10 parity required.

# ======================================================================
2. MY HOME PAGE — COMPLETE REBUILD
CURRENT FAILURE:

- not loading
- incorrect data
- wrong purpose
- not matching web app
- likely dashboard clone

---
CORRECT WEB SEMANTICS:

My Home =
current user's own marketplace listings/posts ONLY.

NOT:

- feed clone
- dashboard
- analytics page
- profile clone

---
IMPLEMENT FROM WEB APP:

- ownership filtering
- listing cards
- edit/delete actions
- status indicators
- user-owned data only
- API filtering
- loading states
- empty states

---
MANDATORY:

Inspect actual APIs and ownership logic from web app source code.

NO assumptions allowed.

---
TARGET SCORE:
10/10 parity required.

# ======================================================================
3. FOR YOU PAGE — COMPLETE REBUILD
CURRENT FAILURE:

- crashing earlier
- duplicate of AllPosts
- wrong recommendation behavior
- fake implementation

---
MANDATORY REQUIREMENTS:

Analyze actual recommendation logic in web app.

ForYou MUST:

- NOT duplicate AllPosts
- NOT behave as generic listing page
- use recommendation/personalization flow

---
IMPLEMENT:

- actual recommendation rendering
- personalized filtering
- category relevance
- recommendation cards
- API parity
- sorting parity
- state handling parity

---
MANDATORY:

If Android implementation differs from web behavior:
DELETE and rebuild.

---
TARGET SCORE:
10/10 parity required.

# ======================================================================
4. SALE DONE PAGE — COMPLETE REBUILD
CURRENT FAILURE:

- incorrect UI
- wrong functionality
- fake “sold history” implementation
- incorrect buyer flow
- wrong dropdown behavior
- reason dropdown empty
- not matching web app

---
CRITICAL ISSUE:

Android created invented logic not existing in web app.

---
MANDATORY:

Inspect actual web app:

- dropdown options
- workflows
- validation rules
- confirmation logic
- API payloads

---
REQUIRED:

- exact same dropdown options
- same validations
- same action flow
- same permissions
- same UI structure
- same modal/dialog flow

---
IMPORTANT:

Web app DOES NOT contain fake “Sold History” logic currently added in Android.

REMOVE invented features immediately.

---
TARGET SCORE:
10/10 parity required.

# ======================================================================
5. SALE UNDONE PAGE — COMPLETE REBUILD
CURRENT FAILURE:

- reason dropdown empty
- API mismatch
- incorrect logic
- wrong forms
- different workflow from web app

---
MANDATORY:

Inspect:

- reason APIs
- dropdown population
- validation rules
- submission flow
- confirmation states

---
IMPLEMENT:

- actual dropdown population logic
- API integration
- proper validation
- exact web parity UX

---
FAIL CONDITIONS:

- empty dropdown
- missing reason values
- incorrect payload
- incorrect API route

---
TARGET SCORE:
10/10 parity required.

# ======================================================================
6. FEEDBACK PAGE — COMPLETE REBUILD
CURRENT FAILURE:

- not matching web app
- incorrect forms
- wrong UI hierarchy
- incorrect fields

---
MANDATORY:

Clone from actual web app:

- layout
- field order
- validations
- CTA behavior
- success/error flows

---
NO guessed forms allowed.

---
TARGET SCORE:
10/10 parity required.

# ======================================================================
COMPLAINTS PAGE
STATUS:
Partial parity achieved (~50%).

Still requires:

- exact spacing
- exact hierarchy
- exact workflows
- API parity
- responsive behavior
- loading/error state parity

DO NOT MARK COMPLETE YET.

# ======================================================================
MANDATORY SCREENSHOT COMPARISON PROCESS
FOR EVERY PAGE:

1. Login into localhost web app
2. Capture screenshots:desktop view
3. responsive/mobile layout
4. dialogs/modals
5. dropdowns
6. loading states
7. Then compare Android implementation side-by-side.

---
MANDATORY RULE:

IF UI differs visibly:
implementation is NOT COMPLETE.

# ======================================================================
MANDATORY CODE ANALYSIS REQUIREMENTS
The team MUST inspect from web app source code:

- routes
- APIs
- components
- hooks
- stores/state
- services
- filters
- payload structures
- conditional rendering
- auth checks
- ownership logic

---
DO NOT:

- guess
- approximate
- simplify
- redesign independently

# ======================================================================
STRICT REBUILD RULE
IF ANY PAGE:

- not loading
- crashing
- structurally incorrect
- semantically incorrect
- visually incorrect
- API mismatched

THEN:

DELETE Android implementation completely
AND
REBUILD FROM SCRATCH USING WEB APP.

# ======================================================================
ZERO BUG / ZERO ERROR / ZERO VULNERABILITY REQUIREMENT
FOR EVERY PAGE ENSURE:

- zero crashes
- zero blank screens
- zero null errors
- zero API mismatch
- zero stale states
- zero broken navigation
- zero dropdown failures
- zero incorrect payloads
- zero lifecycle issues
- zero memory leaks

---
SECURITY REQUIREMENTS:

- validate all inputs
- sanitize payloads
- secure token handling
- prevent unauthorized access
- prevent API abuse
- no insecure local storage

# ======================================================================
MANDATORY GAP ANALYSIS
FOR EACH PAGE PROVIDE:

| Page | Web Parity % | Missing Features | UI Drift | API Drift | Logic Drift | Status |

---
DO NOT MARK:
“Implemented”

UNTIL:
100/100 parity achieved.

# ======================================================================
MANDATORY EXECUTION ORDER

1. Feed
2. My Home
3. For You
4. Sale Done
5. Sale Undone
6. Feedback
7. Complaints refinement

ONLY AFTER:
each page reaches 10/10 parity score.

# ======================================================================
FINAL ACCEPTANCE CRITERIA
DO NOT MARK COMPLETE IF:

- any page still crashes
- any dropdown empty
- any page not loading
- UI differs from web
- API differs from web
- semantics differ from web
- Android contains invented logic
- Android contains extra fake features

# ======================================================================
FINAL GOAL
The Android app must become:

- exact mobile extension of web app
- semantically identical
- visually aligned
- API aligned
- behavior aligned
- production stable

with:

- 100% parity
- 10/10 implementation quality
- zero fake implementations
- zero guessed functionality
- zero placeholder logic
- zero bugs
- zero crashes
- zero UI drift
- zero navigation inconsistencies

The final result must feel like:
the actual MHUB web platform running natively on Android,
NOT a partially recreated approximation.