# App Layer

This folder is reserved for app composition concerns:

- top-level providers
- routing composition
- bootstrap wiring

Business features should stay in `src/features`, and reusable primitives should stay in `src/shared`.
