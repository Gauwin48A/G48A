// src/components/ui/sonner.jsx
import React from "react";

import { useTranslation } from 'react-i18next';

export const Toaster = () => {
  const { t } = useTranslation();
  return null; // Placeholder for "sonner" toast if you're using it
};
