# Shared Layer

Use this folder for reusable, cross-feature building blocks:

- design-system primitives
- shared hooks
- shared utilities
- common constants and adapters

Rule of thumb: if two or more features need it, it belongs in shared.
