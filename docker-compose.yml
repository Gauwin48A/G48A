version: "3.9"

services:
  server:
    build:
      context: ./server
    env_file:
      - ./server/.env
    ports:
      - "5001:5001"

  client:
    build:
      context: ./client
    ports:
      - "4173:4173"
    depends_on:
      - server
